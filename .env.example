# Server/evaluation runner only. No actual credentials in Git.
NEBIUS_API_KEY=
NEBIUS_MODEL=
SLNG_API_KEY=
SLNG_STT_MODEL=
# Optional spoken questions only; not required for STT dialogue.
SLNG_TTS_MODEL=
ARTIFACT_ROOT=./artifacts
# Optional. Empty = defaults in engine/providers/common.py (vision gemma-3-27b-it, text Qwen3-235B).
NEBIUS_TEXT_MODEL=
# Text/tool model for the video reference agent; defaults to NEBIUS_TEXT_MODEL.
NEBIUS_VIDEO_MODEL=
